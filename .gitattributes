# Shell scripts must always use LF line endings, even when checked out on
# Windows. bash fails at runtime with "$'\r': command not found" if a script
# has CRLF endings, so these are pinned regardless of core.autocrlf.
*.sh text eol=lf
*.sh.tpl text eol=lf
