# ai-prototype-accelerator — Python Dependencies
# Static scaffold file — do NOT modify when processing spec.yaml.

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
websockets==12.0

# Azure SDKs
azure-identity==1.17.0
azure-storage-blob==12.20.0
# Microsoft Agent Framework v1.3.0 — Responses-protocol (non-classic) Foundry agents
agent-framework-core>=1.3.0
agent-framework-foundry>=1.3.0
# Azure AI Projects v2.x — used by both MAF runtime AND agents/register_agents.py
# (registration calls AIProjectClient.agents.create_version() with PromptAgentDefinition)
azure-ai-projects>=2.1.0
openai>=1.75.0                   # Responses API client (pulled in by azure-ai-projects)
aiohttp>=3.13.5                   # async transport for azure-core (required by MAF async AIProjectClient)

# Database — Cosmos DB NoSQL
azure-cosmos>=4.7.0

# Utilities
pydantic==2.7.0
python-dotenv==1.0.1
httpx==0.27.0
pyyaml==6.0.1

# Observability
# Do NOT pin opentelemetry-* packages here — the azure-monitor-opentelemetry
# distro declares its own compatible OTel sdk/instrumentation versions
# (including opentelemetry-instrumentation-fastapi). Co-pinning them forced
# pip to backtrack to azure-monitor-opentelemetry 1.8.2 alongside
# opentelemetry-sdk 1.44.0 (released 2026-07-16), whose exporter still
# imports the removed `opentelemetry.sdk._logs.LogData` — so
# configure_azure_monitor() raised ImportError at startup and the app ran
# with zero telemetry (swallowed by main.py's defensive try/except).
azure-monitor-opentelemetry>=1.8.9
